# Music IR & JAMS Field Guide

This document is the definitive reference for interpreting, querying, and consuming `music-ir.json` and `analysis.jams` files generated by the Agent Listening pipeline. It is designed for both human engineers and downstream multimodal music agents.

---

## 1. Dual-Artifact Overview

Every audio track processed by the pipeline produces two complementary artifacts:

| Artifact | File Name | Target Consumer | Primary Role |
| :--- | :--- | :--- | :--- |
| **Music IR** | `<track-id>.music-ir.json` | Downstream Multimodal Agents / Synthesis Systems | **Domain-aligned, concise (<10KB) structural representation** for high-level musical reasoning and parametric control. |
| **JAMS Archive** | `<track-id>.analysis.jams` | Audio Researchers / Verification Tools | **Schema-validated time-series evidence** preserving timing observations, frame vectors, multi-candidate hypotheses, and tool provenance. |

---

## 2. Field Specifications (`music-ir.json`)

### 2.1 Track Metadata (`track`)

```json
"track": {
  "id": "my-track-001",
  "title": "Track Title",
  "source_file": "source/my-track-001.wav",
  "duration_s": 183.42,
  "analysis_mode": "full_mix"
}
```

- **`id`** (`string`): Unique identifier for the audio asset.
- **`source_file`** (`string`): Relative path to the original raw audio file.
- **`duration_s`** (`float`): Total track duration in seconds (rounded to 2 decimal places).
- **`analysis_mode`** (`string`): Execution profile (`full_mix`, `stem`, or `solo`).

---

### 2.2 Global Parameters (`global`)

```json
"global": {
  "tempo_bpm": {
    "value": 84.0,
    "evidence": [
      {"tool": "allin1", "field": "bpm", "value": 84.0},
      {"tool": "essentia", "field": "rhythm.bpm", "value": 84.2}
    ]
  },
  "meter": {"value": null, "status": "not_inferred_in_v0_1"},
  "key_summary": "D minor",
  "key_candidates": [
    {"algorithm": "edma", "key": "D", "scale": "minor", "strength": 0.88, "tool": "essentia"},
    {"algorithm": "temperley", "key": "D", "scale": "minor", "strength": 0.79, "tool": "essentia"},
    {"algorithm": "krumhansl", "key": "F", "scale": "major", "strength": 0.65, "tool": "essentia"}
  ]
}
```

- **`tempo_bpm.value`** (`float`): The estimated global tempo in beats per minute.
- **`tempo_bpm.evidence`** (`array`): Cross-tool corroboration from `allin1` and `Essentia`.
- **`key_summary`** (`string`): The winning tonality hypothesis (e.g., `"D minor"`), resolved via maximum strength arbitration ([ADR-0009](file:///Users/dmus/Transverse%20Sound%20Lab/Agent%20Listening/docs/adr/0009-key-arbitration-via-max-strength.md)).
- **`key_candidates`** (`array`): Full list of tonality hypotheses across algorithms (EDMA, Temperley, Krumhansl) with normalized `strength` scores in `[0.0, 1.0]`.

> [!NOTE]
> **Downstream Agent Sense**: Use `key_summary` as the default pitch center. If the track is ambiguous or modal, inspect `key_candidates` to see if relative major/minor ambiguity exists (e.g., D minor vs F major).

---

### 2.3 Structural Time Grid & Sections (`structure`)

```json
"structure": {
  "beats_s": [0.45, 1.16, 1.88, 2.59, 3.30],
  "downbeats_s": [0.45, 3.30],
  "sections": [
    {
      "start_s": 0.0,
      "end_s": 17.2,
      "label": "intro",
      "tool": "allin1",
      "confidence": null,
      "loudness_lufs": -18.2,
      "spectral_centroid_hz": 1200.4,
      "dynamic_complexity": 1.8
    }
  ]
}
```

- **`beats_s`** (`array[float]`): Exact second timestamps of detected rhythmic beat onsets.
- **`downbeats_s`** (`array[float]`): Timestamps of measure-initiating accents (Bar 1, Beat 1).
- **`sections`** (`array[object]`): Contiguous functional musical segments:
  - **`start_s` / `end_s`**: Temporal boundaries in seconds.
  - **`label`**: Functional role (`intro`, `verse`, `chorus`, `bridge`, `outro`, `instrumental`).
  - **Inline Acoustic Descriptors** ([ADR-0008](file:///Users/dmus/Transverse%20Sound%20Lab/Agent%20Listening/docs/adr/0008-inline-section-acoustic-summary.md)): Local segment loudness, spectral brightness, and dynamics for direct state transition control.

---

### 2.4 Harmony (`harmony`)

```json
"harmony": {
  "key_summary": "D minor",
  "chord_statistics": {
    "histogram": {"Dm": 0.45, "Bb": 0.30, "Gm": 0.25},
    "changes_rate": 0.35,
    "source": "essentia"
  },
  "time_aligned_chords": {
    "enabled": false,
    "status": "optional_chordino_or_other_aligner_required"
  }
}
```

- **`chord_statistics.histogram`**: Essentia's normalized chord histogram, preserved as the extractor emits it (typically a numeric array; fixture data may use a labeled object).
- **`chord_statistics.changes_rate`**: Harmonic rhythm speed (approximate chord transitions per second).
- **`time_aligned_chords.enabled`**: Set to `false` in V0.1 ([ADR-0006](file:///Users/dmus/Transverse%20Sound%20Lab/Agent%20Listening/docs/adr/0006-harmony-key-commitment.md)).

---

### 2.5 Acoustic & Psychoacoustic Descriptors (`audio_features`)

```json
"audio_features": {
  "loudness_ebu128_integrated_lufs": -13.4,
  "loudness_range_lu": 7.8,
  "dynamic_complexity": 2.6,
  "spectral_centroid_hz_mean": 1546.6,
  "spectral_flux_mean": 0.1068,
  "onset_rate_per_s": 1.2,
  "notes": "Extracted via Essentia profile 'essentia_v0_1'..."
}
```

| Field | Physical / Psychoacoustic Meaning | Downstream Interpretation Sense |
| :--- | :--- | :--- |
| **`loudness_ebu128_integrated_lufs`** | Integrated broadcast loudness (EBU R128). | General perceived loudness / volume level. |
| **`loudness_range_lu`** | Statistical variation in macro loudness across the track. | Dynamic drama: small (<4 LU) = heavy compressed; large (>10 LU) = expressive/classical. |
| **`dynamic_complexity`** | Ratio of average loudness variation over short frames. | Internal rhythmic pulse depth vs sustained drone texture. |
| **`spectral_centroid_hz_mean`** | Center of mass of the frequency spectrum (Hz). | Perceived timbre brightness / darkness (filter cutoff proxy). |
| **`spectral_flux_mean`** | Frame-to-frame spectral distance. | Rate of sonic change / transient evolution. |
| **`onset_rate_per_s`** | Detected note/percussive event attacks per second. | Rhythmic event density (Sparse vs Busy texture). |

---

### 2.6 Interpretation & Hints (`interpretation`)

```json
"interpretation": {
  "manual_notes": [],
  "derived_summary": {"enabled": false, "rule": "Can only be generated from evidence and manual notes (ADR-0004)."},
  "synthesis_hints": {
    "enabled": false,
    "notes": "Synthesis mapping delegated to downstream agent (ADR-0004)."
  }
}
```

- **`synthesis_hints`**: Reserved for high-level generative intentions (e.g. macro energy curves, spatial width tendencies). **Never hardcodes specific sound synthesis code or class names.**

---

### 2.7 Provenance & Review (`provenance`, `review`)

```json
"provenance": {
  "source": {"sha256": "..."},
  "allin1": {"version": "3.1.0", "raw_json": "raw/allin1.json", "raw_sha256": "..."},
  "essentia": {"version": "2.1-beta6-git", "profile": "profiles/essentia_v0_1.yaml", "profile_sha256": "...", "raw_json": "raw/essentia.json", "raw_sha256": "..."},
  "created_at": "2026-08-22T15:23:34Z"
},
"review": {
  "human_checked": false,
  "known_uncertainties": ["Segment boundaries and key candidates require auditory verification."]
}
```

- Records content hashes and whether an engineer has listened to and verified the analysis. Existing artifacts are protected by default; `--overwrite` intentionally replaces them.

JAMS observations retain unknown confidence as JSON `null`. The document passes the official JAMS base schema; namespace-strict confidence validation is not claimed because neither extractor supplies a numeric confidence for every observation.
Native Essentia frame pools that omit timestamps remain available in raw evidence but are not time-aligned or aggregated into Music IR.
